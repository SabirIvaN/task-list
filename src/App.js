import * as wrap from './ui/blocks/wrap/wrap.js';          // Importing the wrap component
import * as app from './ui/blocks/app/app.js';             // Importing the app component
import * as header from './ui/blocks/header/header.js';    // Importing the header component
import * as main from './ui/blocks/main/main.js';          // Importing the main component
import * as list from './ui/blocks/list/list.js';          // Importing the list component
import * as listItem from './ui/blocks/item/item.js';      // Importing the item component
